name = "merajmirzaei-site"
main = "src/worker.js"
compatibility_date = "2024-09-23"

[assets]
directory = "./merajmirzaei-site (4)"
binding = "ASSETS"
run_worker_first = true

# Trivial redeploy trigger (2026-09-03): pick up the ADMIN_PASSWORD and
# GITHUB_TOKEN secrets that were added to the Cloudflare project after the
# last deployment.
